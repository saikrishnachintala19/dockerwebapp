---
version: "3"
services:
 devopsdb:
   container_name: devopsdb
   image: dbimage
   ports:
     - "3306:3306"

 application:
   container_name: appcontainer
   image: appimage
   ports:
     - "8080:8080"
   depends_on:
     - "devopsdb"
